# 本地 Issues

本仓库在 `docs/issues/` 下维护本地文件化 issue 追踪。

规则：

- 一个 Markdown 文件对应一个 issue
- open issue 放在 `docs/issues/open/`
- closed issue 移到 `docs/issues/closed/`
- 文件名优先使用 `NNNN-slug.md`

本地 issue 用来记录范围明确的实现工作、后续风险和需要落进文档的决策，避免这些内容消失在聊天记录里。
